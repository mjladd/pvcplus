tool = "spectwarper"
input = "sine440_2s_44k.wav"
description = "nonlinear warp curve index"
legacy_steps = [
    "spectwarper -W2 {input} {output}",
]
output_ext = "wav"
tolerance = { kind = "spectral", max_db_error = 0.5 }
