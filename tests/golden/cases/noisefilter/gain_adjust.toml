tool = "noisefilter"
input = "noise_then_tone_44k.wav"
description = "denoise with a stronger noise-threshold gain"
legacy_steps = [
    "noisefilter -B0 -E0.5 -S12 {input} {output}",
]
output_ext = "wav"
tolerance = { kind = "spectral", max_db_error = 0.5 }
