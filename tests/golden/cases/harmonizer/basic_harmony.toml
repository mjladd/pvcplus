tool = "harmonizer"
input = "sine440_2s_44k.wav"
description = "fifth + octave harmony from a data table"
legacy_steps = [
    "harmonizer -F{fixtures}/harmonizer_table.txt {input} {output}",
]
output_ext = "wav"
tolerance = { kind = "spectral", max_db_error = 0.5 }
notes = "Data-table-driven case (plan's Task 1.2 requirement)."
