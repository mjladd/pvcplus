tool = "envelope"
input = "sine440_2s_44k.wav"
description = "amplitude envelope, ASCII output"
legacy_steps = [
    "envelope {input} {output}",
]
output_ext = "txt"
tolerance = { kind = "numeric", max_abs_error = 1e-3 }
